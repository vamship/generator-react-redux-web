/**
 * Application configuration.
 */

// const targetEnv = 'development';
const _debugMode = true;

/**
 * Base url to API used by the website.
 */
export const apiRoot = 'http://localhost:4000';

/**
 * Flag that determines if Redux logger is enabled.
 */
export const loggingEnabled = _debugMode;
